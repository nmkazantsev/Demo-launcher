pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = 'Demo'
include('core')
project(':core').projectDir = file('../sealEngine_3M/core')
include('desktop')
project(':desktop').projectDir = file('../sealEngine_3M/desktop')
include 'game'
