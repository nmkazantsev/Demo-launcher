pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = 'Demo'
include('core')
project(':core').projectDir = file('../seal-runtime-worktree/core')
include('desktop')
project(':desktop').projectDir = file('../seal-runtime-worktree/desktop')
include 'game'
